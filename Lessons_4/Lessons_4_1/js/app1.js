for (let numStart = 10; numStart <= 20; numStart++) {
    if (numStart < 20) {
        document.write(`${numStart}, `)
    };
    if (numStart === 20) {
        document.write(`${numStart}.`) 
    };
};